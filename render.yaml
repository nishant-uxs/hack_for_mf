services:
  # Full Stack Application - Backend + Frontend with SQLite
  - type: web
    name: civicsense-app
    env: node
    plan: free
    buildCommand: cd client && npm install && npm run build && cd ../server && npm install
    startCommand: cd server && npm start
    autoDeploy: true
    healthCheckPath: /api/health
    
    # Serve static files from client build
    staticPublishDir: client/build
    
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: CLIENT_URL
        value: https://civicsense-app.onrender.com
      - key: ADMIN_EMAIL
        value: agarwalnishant812@gmail.com
      - key: GEMINI_API_KEY
        sync: false
